using FairAI;

while (true)
{
    var request = Console.ReadLine();
    ILanguage dm;
    IDepth depth;
    ICore core;
    //up
    IDepth depth2;
    ILanguage dm2;
    var response = "answer: "+ request;
    Console.WriteLine(response);
}